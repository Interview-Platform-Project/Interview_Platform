/**
 * Снимок состояния редактора комнаты — то, что сервер рассылает клиентам
 * (события `code_state` и `code_update`).
 */
export class EditorStateDto {
  /** Текущее содержимое редактора комнаты. */
  content!: string;

  /** Текущий выбранный язык программирования. */
  language!: string;
}